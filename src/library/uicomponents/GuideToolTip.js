import React from 'react';

const GuideToolTip = ({
  continuous,
  index,
  step,
  backProps,
  closeProps,
  primaryProps,
  tooltipProps,
}) => {
  return <h1>{step.title}</h1>;
};

export default GuideToolTip;
